# tabslx - TabsLite Extractor Default Configuration
# Copy to config.yaml and customize

json: tabslite_backup.json
data: data
cache: true

filename:
  lowercase: false
  space: "-"
  id: true
  format: "{artist}---{song}-{id}.txt"
